import styles from "./SCCSHeader.module.css"

export default function SCCSHeader(){
    return (
        <header className={styles.SCCSHeader + " h-[60px] bg-primary"}>
        
        </header>
    );
}